Vidara Figma HTML — fixed 1280px width
======================================

Use these files when your Figma frames are 1280px wide (same as teammates).

- Entire layout is wrapped in a 1280px-wide column (horizontal scroll disabled inside the frame).
- Viewport meta is set to width=1280 for consistent browser zoom.
- Responsive breakpoints removed: layout matches desktop at 1280px only.

Open in Chrome, or import via html.to.design using a local server.

Files mirror ../ (parent folder) but locked to 1280px.
